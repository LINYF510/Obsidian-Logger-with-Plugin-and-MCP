# Obsidian Logger - MCP Server

> Model Context Protocol (MCP) Server：为 Cursor AI 提供 12个强大的日志分析和 Auto-Reload 管理工具

[![Python](https://img.shields.io/badge/Python-3.8%2B-blue)](https://www.python.org/)
[![MCP](https://img.shields.io/badge/MCP-0.1.0%2B-green)](https://modelcontextprotocol.io/)
[![Tests](https://img.shields.io/badge/tests-47%20passing-brightgreen)]()
[![Coverage](https://img.shields.io/badge/coverage->80%25-brightgreen)]()

---

## ✨ 功能概览

MCP Server 是连接 Obsidian Logger 插件和 Cursor AI 的智能中间层，提供：

- 🔍 **智能日志分析**：自动错误分类、模式识别、修复建议
- 📊 **实时统计监控**：日志级别分布、错误趋势分析
- 🔄 **Auto-Reload 管理**：远程控制插件重载、查看统计
- ⚡ **高性能缓存**：多层缓存系统，响应时间 < 500ms
- 📡 **实时文件监听**：自动检测日志变化，保持数据最新

---

## 🛠️ 工具集（12个）

### 📝 日志工具（6个）

| 工具 | 功能 | 参数 | 返回 |
|------|------|------|------|
| `read_logs` | 读取最近日志 | `lines`（行数）<br>`level`（级别过滤） | 格式化的日志内容 |
| `get_log_summary` | 获取统计摘要 | 无 | 总数、各级别数量、占比 |
| `get_recent_errors` | 获取最近错误 | `count`（错误数量） | 最近的错误日志 |
| `analyze_errors` | 深度错误分析 | 无 | 错误分类、频率统计<br>常见模式、修复建议 |
| `get_log_file_path` | 获取日志路径 | 无 | 日志文件绝对路径 |
| `clear_logs` | 清空日志文件 | 无 | 操作结果（自动备份） |

### 🔄 Auto-Reload 管理工具（6个）

| 工具 | 功能 | 参数 | 返回 |
|------|------|------|------|
| `get_auto_reload_status` | 获取状态和监控列表 | 无 | 当前模式、监控插件列表 |
| `get_auto_reload_mode` | 获取当前监控模式 | 无 | auto/smart/manual |
| `set_auto_reload_mode` | 切换监控模式 | `mode`（模式名称） | 切换结果 |
| `manage_watched_plugins` | 管理监控列表 | `action`（操作类型）<br>`plugin_id`（插件ID）<br>`plugin_ids`（插件列表） | 操作结果 |
| `trigger_plugin_reload` | 手动触发重载 | `plugin_id`（插件ID） | 重载结果 |
| `get_reload_statistics` | 获取重载统计 | 无 | 各插件重载次数、成功率 |

详细 API 文档：[MCP-Tools-API.md](../docs/api/MCP-Tools-API.md)

---

## 🚀 安装指南

### 方式一：uvx 一键安装（推荐）

无需手动安装，uvx 会自动从 GitHub 获取最新版本：

```bash
# 1. 安装 uv
pip install uv

# 2. 创建配置文件
curl -O https://raw.githubusercontent.com/LINYF510/Obsidian-Logger-with-Plugin-and-MCP/main/mcp-server/create-config.py
python create-config.py

# 3. 配置 Cursor（见下方）
```

### 方式二：本地安装（开发者）

```bash
# 1. 克隆仓库
git clone https://github.com/LINYF510/Obsidian-Logger-with-Plugin-and-MCP.git
cd Obsidian-Logger-with-Plugin-and-MCP/mcp-server

# 2. 安装依赖
pip install -r requirements.txt

# 3. 开发依赖（可选）
pip install -r requirements-dev.txt

# 4. 安装为可编辑包
pip install -e .

# 5. 创建配置
python create-config.py
```

---

## ⚙️ 配置

### 自动配置（推荐）

使用配置向导自动检测和生成配置：

```bash
python create-config.py
```

向导功能：
- ✅ 自动检测系统中的 Obsidian Vault
- ✅ 自动读取插件日志配置
- ✅ 智能路径处理（跨平台兼容）
- ✅ 交互式选择和验证

### 手动配置

复制示例配置并编辑：

```bash
cp config.example.json config.json
```

配置文件结构：

```json
{
  "log_file_path": "/path/to/vault/../obsidian-logger/obsidian-debug.log",
  "vault_path": "/path/to/vault",
  "cache_size": 1000,
  "cache_ttl_seconds": 300,
  "file_monitor": {
    "enabled": true,
    "debounce_ms": 100
  },
  "logging": {
    "level": "INFO",
    "format": "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  }
}
```

### Cursor MCP 配置

编辑 Cursor 配置文件 (`~/.config/Cursor/User/settings.json`)：

#### uvx 模式（推荐）

```json
{
  "mcpServers": {
    "obsidian-logger": {
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/LINYF510/Obsidian-Logger-with-Plugin-and-MCP",
        "obsidian-logger-mcp",
        "/absolute/path/to/config.json"
      ],
      "description": "Obsidian 日志收集和 Auto-Reload 服务",
      "transport": "stdio"
    }
  }
}
```

#### 本地模式（开发）

```json
{
  "mcpServers": {
    "obsidian-logger": {
      "command": "python",
      "args": [
        "/absolute/path/to/mcp-server/src/mcp_obsidian_logger.py",
        "/absolute/path/to/config.json"
      ],
      "description": "Obsidian 日志收集和 Auto-Reload 服务",
      "transport": "stdio"
    }
  }
}
```

---

## 🏗️ 技术架构

### 系统架构

```
┌─────────────────────────────────────────────┐
│         Cursor AI（应用层）                  │
│  - 用户交互界面                              │
│  - @obsidian-logger 工具调用                │
└─────────────────┬───────────────────────────┘
                  │ JSON-RPC 2.0 (MCP 协议)
                  ↓
┌─────────────────────────────────────────────┐
│        MCP Server（本模块）                  │
│  ┌─────────────────────────────────────┐   │
│  │  Tool Router（工具路由）             │   │
│  │  - 12 个工具接口                     │   │
│  │  - 参数验证                          │   │
│  │  - 错误处理                          │   │
│  └────┬──────────────────────────┬─────┘   │
│       │                          │          │
│  ┌────▼─────────┐        ┌──────▼────┐     │
│  │ Log Manager  │        │ Config    │     │
│  │ - 读取日志   │        │ Manager   │     │
│  │ - 分析错误   │        │ - 配置加载│     │
│  │ - 统计信息   │        │ - 监听变更│     │
│  └──────┬───────┘        └───────────┘     │
│         │                                   │
│  ┌──────▼───────┐       ┌────────────┐     │
│  │   Cache      │       │File Monitor│     │
│  │ - 日志缓存   │       │- watchdog  │     │
│  │ - 统计缓存   │       │- 事件处理  │     │
│  │ - TTL 管理   │       │- 缓存更新  │     │
│  └──────────────┘       └────────────┘     │
└─────────────────┬───────────────────────────┘
                  │ 文件系统 API
                  ↓
┌─────────────────────────────────────────────┐
│  Obsidian Logger 插件 + 日志文件             │
│  - data.json（插件配置）                     │
│  - obsidian-debug.log（日志文件）            │
└─────────────────────────────────────────────┘
```

### 核心模块

#### 1. LogManager（日志管理器）

```python
class LogManager:
    """日志文件管理和分析"""
    
    def read_logs(self, lines: int, level: str) -> List[str]:
        """读取并过滤日志"""
        
    def analyze_errors(self) -> Dict:
        """深度错误分析"""
        
    def get_summary(self) -> Dict:
        """生成统计摘要"""
```

**功能**：
- 高效读取大文件（使用 tail 策略）
- 日志级别过滤
- 错误模式识别
- 统计信息生成

#### 2. ConfigManager（配置管理器）

```python
class ConfigManager:
    """配置文件管理和监听"""
    
    def load_config(self) -> Dict:
        """加载配置文件"""
        
    def watch_config(self, callback):
        """监听配置变更"""
        
    def get_plugin_config(self, plugin_id: str) -> Dict:
        """读取插件配置"""
```

**功能**：
- 配置文件加载和验证
- 实时监听插件配置变更
- 自动同步到缓存

#### 3. Cache（缓存系统）

```python
class Cache:
    """多层缓存系统"""
    
    def __init__(self, max_size: int = 1000):
        self.log_entries = deque(maxlen=max_size)  # 环形缓冲区
        self.error_stats = {}                      # 错误统计
        self.file_metadata = {}                    # 文件元数据
        self.search_cache = TTLCache(...)          # TTL缓存
```

**功能**：
- 日志条目缓存（最多1000条）
- 错误统计缓存
- 搜索结果缓存（5分钟TTL）
- 文件元数据缓存

#### 4. FileMonitor（文件监听器）

```python
class FileMonitor:
    """基于 watchdog 的文件监听"""
    
    def __init__(self, log_file_path: str):
        self.observer = Observer()
        
    def start(self):
        """启动监听"""
        
    def on_modified(self, event):
        """处理文件修改事件"""
```

**功能**：
- 实时监听日志文件变化
- 事件去重和防抖
- 自动更新缓存

---

## 🧪 测试

### 测试覆盖

- **总测试用例**：47个
- **覆盖率**：> 80%
- **测试框架**：pytest

### 测试模块

| 模块 | 测试用例 | 说明 |
|------|---------|------|
| LogManager | 20个 | 日志读取、分析、统计 |
| ConfigManager | 16个 | 配置加载、监听、同步 |
| Cache | 11个 | 缓存操作、TTL、失效 |

### 运行测试

```bash
# 运行所有测试
pytest

# 详细输出
pytest -v

# 生成覆盖率报告
pytest --cov=src --cov-report=html
# 报告生成在 htmlcov/index.html

# 运行特定测试
pytest tests/test_log_manager.py

# 运行特定测试函数
pytest tests/test_log_manager.py::test_read_logs
```

---

## 🔧 开发

### 开发环境设置

```bash
# 1. 安装开发依赖
pip install -r requirements-dev.txt

# 2. 安装预提交钩子（可选）
pre-commit install

# 3. 配置 IDE
# VSCode: 安装 Python、Pylance 插件
# PyCharm: 配置 Python 解释器
```

### 常用命令

```bash
# 代码格式化
black src/ tests/

# 代码检查
flake8 src/ tests/

# 类型检查
mypy src/

# 运行测试
pytest

# 生成覆盖率报告
pytest --cov=src --cov-report=html

# 清理缓存
find . -type d -name __pycache__ -exec rm -rf {} +
find . -type f -name "*.pyc" -delete
```

### 项目结构

```
mcp-server/
├── src/
│   ├── __init__.py                    # 包初始化
│   ├── mcp_obsidian_logger.py         # 主程序和工具定义
│   ├── config_manager.py              # 配置管理
│   ├── log_manager.py                 # 日志文件管理
│   ├── cache.py                       # 缓存系统
│   └── file_monitor.py                # 文件监听
│
├── tests/
│   ├── conftest.py                    # pytest 配置和 fixtures
│   ├── test_log_manager.py            # LogManager 测试
│   ├── test_config_manager.py         # ConfigManager 测试
│   └── test_cache.py                  # Cache 测试
│
├── config.example.json                # 配置示例
├── create-config.py                   # 配置向导
├── verify-paths.py                    # 路径验证工具
├── requirements.txt                   # 运行时依赖
├── requirements-dev.txt               # 开发依赖
├── pyproject.toml                     # Python 包配置（支持 uvx）
├── setup.py                           # 安装脚本
└── pytest.ini                         # pytest 配置
```

---

## 📊 性能指标

| 指标 | 目标 | 实际 | 状态 |
|------|------|------|------|
| 工具响应时间 | < 500ms | ~200ms | ✅ |
| 缓存命中率 | > 80% | ~85% | ✅ |
| 内存占用 | < 100MB | ~60MB | ✅ |
| CPU 占用 | < 5% | ~2% | ✅ |
| 并发支持 | 10+ | ✅ | ✅ |

---

## 📚 相关文档

- [项目总览](../README.md)
- [用户手册](../USER_MANUAL.md)
- [开发指南](../DEVELOPMENT.md)
- [MCP Tools API](../docs/api/MCP-Tools-API.md)
- [完整开发方案](../docs/Obsidian%20Logger%20项目开发方案.md)

---

## 🤝 贡献

欢迎贡献！请阅读 [贡献指南](../CONTRIBUTING.md)。

---

## 📄 许可证

MIT License - 查看 [LICENSE](../LICENSE) 文件了解详情

---

**Powered by MCP (Model Context Protocol)**
